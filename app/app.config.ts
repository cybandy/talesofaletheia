export default defineAppConfig({
  ui: {
    colors: {
      primary: 'pink',
      secondary: 'fuchsia',
      neutral: 'neutral'
    },
    input: {
      slots: {
        root: 'w-full'
      }
    },
    inputTags: {
      slots: {
        root: 'w-full'
      }
    },
    textarea: {
      slots: {
        root: 'w-full'
      }
    }
  }
})
